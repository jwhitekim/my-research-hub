# name: Publish Chrome Extension

# on:
#   workflow_dispatch:

# permissions:
#   contents: read

# jobs:
#   publish:
#     runs-on: ubuntu-latest
#     env:
#       CI: false

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Validate manifest
#         run: python -m json.tool extensions/paper/manifest.json > /dev/null

#       - name: Package extension
#         working-directory: extensions/paper
#         run: zip -r ../../extension.zip . -x "*.DS_Store" "__MACOSX/*"

#       - name: Upload & release
#         uses: mnao305/chrome-extension-upload@2.1.0
#         with:
#           file-path: extension.zip
#           extension-id: ${{ secrets.GOOGLE_EXTENSION_ID }}
#           client-id: ${{ secrets.GOOGLE_CLIENT_ID }}
#           refresh-token: ${{ secrets.GOOGLE_REFRESH_TOKEN }}
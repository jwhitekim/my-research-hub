services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: veloo:local
    env_file:
      - .env
    ports:
      - "0.0.0.0:${PORT:-9000}:${PORT:-9000}"
    init: true
    restart: unless-stopped